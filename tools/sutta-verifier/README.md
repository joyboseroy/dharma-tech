# Sutta Verifier

Check whether a quote attributed to the Buddha is actually in the canonical texts.

## Usage

```bash
python sutta_verifier.py 'The mind is everything'
python sutta_verifier.py --check-suspicious
python sutta_verifier.py --interactive
```

→ [Back to main repo](../../README.md)
